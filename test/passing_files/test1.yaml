# name: Test 1
on:
  workflow_dispatch:
    inputs:
      test1: 
        required: false
        default: none
      test2:
        required: false
        default: none
  # bleh: 

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

  testing-one:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3